# Password gate (HTTP Basic auth via middleware). Leave blank locally to skip.
APP_PASSWORD=

# Public GitHub repo holding the scraper + articles.json
GITHUB_OWNER=sethbkrea
GITHUB_REPO=nyc-yimby-tracker

# Token used ONLY to trigger workflow_dispatch. Reads are anonymous (public repo).
# Needs `workflow` + `repo` scope.
GH_TOKEN=
